name: Release Android app

# Two ways in:
#  * push a tag `vX.Y.Z` matching version.properties -> builds, verifies and publishes a GitHub
#    Release with the signed release APK, the debug APK and their checksums;
#  * "Run workflow" (workflow_dispatch) from any branch -> builds and verifies the same signed
#    release APK and attaches it as a workflow artifact only (no tag, no GitHub Release).
# Signing secrets are always required.
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Validate release tag
        if: github.ref_type == 'tag'
        shell: bash
        run: |
          VERSION=$(grep '^VERSION_NAME=' version.properties | cut -d= -f2)
          test "${GITHUB_REF_NAME}" = "v${VERSION}" || {
            echo "Tag ${GITHUB_REF_NAME} does not match version.properties (v${VERSION})."
            exit 1
          }

      - name: Prepare release notes from changelog
        if: github.ref_type == 'tag'
        shell: bash
        run: |
          python3 - <<'PY'
          import os
          import pathlib
          import re

          tag = os.environ["GITHUB_REF_NAME"]
          changelog = pathlib.Path("CHANGELOG.md").read_text(encoding="utf-8")
          section = re.search(
              rf"^## {re.escape(tag)}(?:[ \t]+[^\n]*)?\n(.*?)(?=^## |\Z)",
              changelog, re.MULTILINE | re.DOTALL,
          )
          if section is None or not section.group(1).strip():
              raise SystemExit(f"Missing changelog entry for {tag}")
          pathlib.Path(os.environ["RUNNER_TEMP"], "release-notes.md").write_text(
              section.group(1).strip() + "\n", encoding="utf-8",
          )
          PY

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4
        with:
          packages: platform-tools

      - name: Configure release signing
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          STORE_PASSWORD: ${{ secrets.ANDROID_STORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        shell: bash
        run: |
          test -n "$KEYSTORE_BASE64" && test -n "$STORE_PASSWORD" && \
            test -n "$KEY_ALIAS" && test -n "$KEY_PASSWORD" || {
              echo "Android signing secrets are not configured. See README.md."
              exit 1
            }
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/release.jks"
          cat > keystore.properties <<EOF
          storeFile=$RUNNER_TEMP/release.jks
          storePassword=$STORE_PASSWORD
          keyAlias=$KEY_ALIAS
          keyPassword=$KEY_PASSWORD
          EOF

      - name: Prepare Gradle wrapper
        run: chmod +x gradlew

      - name: Install native toolchain
        run: sdkmanager --install "ndk;26.1.10909125" "cmake;3.22.1"

      - name: Verify and build
        run: ./gradlew lintDebug testDebugUnitTest assembleRelease assembleDebug --console=plain

      - name: Verify APKs and create checksums
        shell: bash
        run: |
          APK_RELEASE=$(ls app/build/outputs/apk/release/*-release.apk | head -n1)
          APK_DEBUG=$(ls app/build/outputs/apk/debug/*-debug.apk | head -n1)
          test -n "$APK_RELEASE" && test -n "$APK_DEBUG"
          APKSIGNER=$(ls -d "$ANDROID_HOME"/build-tools/*/apksigner | sort -V | tail -n1)
          test -n "$APKSIGNER"
          "$APKSIGNER" verify --verbose --print-certs "$APK_RELEASE"
          "$APKSIGNER" verify --verbose --print-certs "$APK_DEBUG"
          sha256sum "$APK_RELEASE" > "${APK_RELEASE}.sha256"
          sha256sum "$APK_DEBUG" > "${APK_DEBUG}.sha256"
          echo "APK_RELEASE=$APK_RELEASE" >> "$GITHUB_ENV"
          echo "APK_DEBUG=$APK_DEBUG" >> "$GITHUB_ENV"

      - name: Upload release APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: DJM-Rec-release-apk
          path: |
            app/build/outputs/apk/release/*-release.apk
            app/build/outputs/apk/release/*-release.apk.sha256
          if-no-files-found: error
          retention-days: 90

      - name: Publish GitHub Release
        if: github.ref_type == 'tag'
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh release create "$GITHUB_REF_NAME"
          "$APK_RELEASE" "$APK_RELEASE.sha256"
          "$APK_DEBUG" "$APK_DEBUG.sha256"
          --verify-tag --notes-file "$RUNNER_TEMP/release-notes.md" --title "$GITHUB_REF_NAME"
