<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!-- Release builds are arm64-v8a only: USB host hardware is 64-bit ARM. Debug builds add
         x86_64 for the emulator; ChromeOS is not a target. -->
    <issue id="ChromeOsAbiSupport" severity="ignore" />

    <!-- Launcher icon shape is intentional (full-bleed design). -->
    <issue id="IconLauncherShape" severity="ignore" />
</lint>
